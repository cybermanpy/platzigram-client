'use strict';

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

const test = require('ava');
const nock = require('nock');
const platzigram = require('../');
const fixtures = require('./fixtures');

let options = {
  endpoints: {
    pictures: 'http://platzigram.test/picture',
    users: 'http://platzigram.test/user',
    auth: 'http://platzigram.test/auth'
  }
};

test.beforeEach(t => {
  t.context.client = platzigram.createClient(options);
});

test('client', t => {
  const client = t.context.client;

  t.is(typeof client.getPicture, 'function');
  t.is(typeof client.savePicture, 'function');
  t.is(typeof client.likePicture, 'function');
  t.is(typeof client.listPictures, 'function');
  t.is(typeof client.listPicturesByTag, 'function');
  t.is(typeof client.saveUser, 'function');
  t.is(typeof client.getUser, 'function');
  t.is(typeof client.auth, 'function');
});

test('getPicture', (() => {
  var _ref = _asyncToGenerator(function* (t) {
    const client = t.context.client;

    let image = fixtures.getImage();

    nock(options.endpoints.pictures).get(`/${image.publicId}`).reply(200, image);

    let result = yield client.getPicture(image.publicId);

    t.deepEqual(image, result);
  });

  return function (_x) {
    return _ref.apply(this, arguments);
  };
})());

test('savePicture', (() => {
  var _ref2 = _asyncToGenerator(function* (t) {
    const client = t.context.client;

    let token = 'xxx-xxx-xxx';
    let image = fixtures.getImage();
    let newImage = {
      src: image.src,
      description: image.description
    };

    nock(options.endpoints.pictures, {
      reqheaders: {
        'Authorization': `Bearer ${token}`
      }
    }).post('/', newImage).reply(201, image);

    let result = yield client.savePicture(newImage, token);

    t.deepEqual(result, image);
  });

  return function (_x2) {
    return _ref2.apply(this, arguments);
  };
})());

test('likePicture', (() => {
  var _ref3 = _asyncToGenerator(function* (t) {
    const client = t.context.client;

    let image = fixtures.getImage();
    image.liked = true;
    image.likes = 1;

    nock(options.endpoints.pictures).post(`/${image.publicId}/like`).reply(200, image);

    let result = yield client.likePicture(image.publicId);

    t.deepEqual(image, result);
  });

  return function (_x3) {
    return _ref3.apply(this, arguments);
  };
})());

test('listPictures', (() => {
  var _ref4 = _asyncToGenerator(function* (t) {
    const client = t.context.client;

    let images = fixtures.getImages(3);

    nock(options.endpoints.pictures).get('/list').reply(200, images);

    let result = yield client.listPictures();

    t.deepEqual(images, result);
  });

  return function (_x4) {
    return _ref4.apply(this, arguments);
  };
})());

test('listPicturesByTag', (() => {
  var _ref5 = _asyncToGenerator(function* (t) {
    const client = t.context.client;

    let images = fixtures.getImages(3);
    let tag = 'platzi';

    nock(options.endpoints.pictures).get(`/tag/${tag}`).reply(200, images);

    let result = yield client.listPicturesByTag(tag);

    t.deepEqual(images, result);
  });

  return function (_x5) {
    return _ref5.apply(this, arguments);
  };
})());

test('saveUser', (() => {
  var _ref6 = _asyncToGenerator(function* (t) {
    const client = t.context.client;

    let user = fixtures.getUser();
    let newUser = {
      username: user.username,
      name: user.name,
      email: 'user@platzigram.test',
      password: 'pl4tzi'
    };

    nock(options.endpoints.users).post('/', newUser).reply(201, user);

    let result = yield client.savePicture(newUser);

    t.deepEqual(result, user);
  });

  return function (_x6) {
    return _ref6.apply(this, arguments);
  };
})());
//# sourceMappingURL=/var/www/html/platzigram-client/node_modules/.cache/ava/ea517e680c106a61d46b95ff79cf9abc.js.map